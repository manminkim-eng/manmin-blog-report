# 작업 지시서 — 블로그 분석 리포트 모바일(밖에서) 접속

> 목표: A-06 "블로그 분석"(`manmin-blog-report`, 다중 페이지 리포트)을 **밖에서 폰으로 열람**. 업무일지·주식과 **동일 방식**(NAS Web Station + Tailscale). 2026-07-13 구축 완료.

## 전제 (공통 인프라, 이미 구축)
- NAS: MANMIN-NAS, LAN 192.168.0.36, 웹루트 `//MANMIN-NAS/web`, Web Station(Nginx 80/443).
- 외부접속 = **Tailscale VPN**만 가능. NAS `manmin-nas` = **100.123.66.123**(키만료 비활성화), 폰 `kim-manmin-s24u`. 계정 manminkim@gmail.com.
- ❌ QuickConnect·File Station 공유링크·포트포워딩 불가/부적합. ✅ Tailscale ON + NAS Tailscale IP.

## 이 리포트의 특징
- **다중 페이지**: `index.html` → `2026-06/`, `youtube/`, `assets/`, `manifest.webmanifest` (상대링크로 연결). → **폴더째** 서빙해야 함(단일 파일 아님).
- charset UTF-8 + viewport 이미 있음(모바일 한글 정상).
- 소스 원본(canonical): `D:/.../60.개발자료[블로그우]/manmin-blog-report/` (최신). 90.분석자료 아래 것은 구버전.

## 1. 웹 배포 (폴더 통째)
```
mkdir -p "//MANMIN-NAS/web/manmin-blog"
cp -r "D:/.../60.개발자료[블로그우]/manmin-blog-report/." "//MANMIN-NAS/web/manmin-blog/"
# 서빙 불필요 파일 정리(선택): build_*.py, __pycache__, data
```

## 2. 접속 URL (현재 작동, 별칭 포털 불필요 — 하위폴더 자동 서빙)
| 위치 | URL |
|---|---|
| 집·사무실(LAN) | `http://192.168.0.36/manmin-blog/` |
| **밖에서(LTE)** | `http://100.123.66.123/manmin-blog/` — **Tailscale ON** 필수 |
- 하위: `/manmin-blog/2026-06/`, `/manmin-blog/youtube/` 도 정상.

## 3. 자동 배포 — ✅ 구현 완료(build_all.py)
- `build_all.py` 끝부분에 **NAS 자동 배포 코드 삽입**됨: `shutil.copytree(REPO, "//MANMIN-NAS/web/manmin-blog", dirs_exist_ok=True, ignore=data/*.py/__pycache__/.git)`. try/except.
- 즉 `python build_all.py --month YYYY-MM` 실행하면 GitHub Push와 별개로 **NAS에도 자동 반영**.
- ⚠️ 경로는 **정슬래시 UNC** `//MANMIN-NAS/web/...`(역슬래시 `\\`는 이 환경 파이썬에서 FileNotFoundError).

## 4. 폰에서 쓰기 + 카카오톡 자동 발송
- Tailscale 앱 ON → 카카오톡 "나에게" 링크 탭(전송됨), 또는 홈화면 아이콘.
- 카톡 메모엔 **LTE 주소만**: `http://100.123.66.123/manmin-blog/`
- **아침 카톡 자동 발송(표준, 2026-07-14)**: 아침 대시보드 예약작업은 완료 시 "요약 + LTE 링크"를 카카오톡 나에게로 발송(업무일지=매일, 주식=매일 P/L). 블로그는 월간이라 **`blog-report-monthly-reminder`(매월 1일 08:00)** 예약이 카톡으로 "데이터 export 후 build_all.py 실행" 리마인더를 보냄.

## 참고 — 3개 대시보드 공통 (NAS Web Station)
- `/brief/` 업무일지 · `/manmin-stock/` 주식 · `/manmin-blog/` 블로그. 모두 Tailscale로 밖에서 열람, 각 파이프라인이 생성 후 자동 배포.
